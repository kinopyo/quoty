$('#<%= params[:ajax_id]%>').popover({title: '<%= message %>', content: '<%= j(link_to "Sign up or login", login_path(rt: request.fullpath)) %>', html: true, trigger: 'manual'}).popover('show');
setTimeout(function() {
  $('#<%= params[:ajax_id]%>').popover('hide');
}, 6000);