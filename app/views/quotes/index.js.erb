$("#quotes").append("<%= j(render(@quotes)) %>");
<% if @quotes.current_page < @quotes.num_pages %>
  $('.pagination').replaceWith('<%= j paginate(@quotes, remote: true) %>');
<% else %>
  $('.pagination').remove();
<% end %>